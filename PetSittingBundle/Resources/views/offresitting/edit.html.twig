{% extends ":Layout:Layout.html.twig" %}

{% block content %}
    <h1 style="margin-left: 650px">Modification de l'offre</h1>
    {% spaceless %}

        <div class="form_row" style="margin-left: 750px">

        {{ form_start(edit_form) }}

        {{ form_widget(edit_form) }}
        <br>
        {{ form_end(edit_form) }}
        <input type="submit" value="Enregistrer la modification" class="btn btn-primary" style="margin-left: 50px" />

    {% endspaceless %}

    </div>
    <ul>
        <li>
            <a href="{{ path('offre_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete" >
            {{ form_end(delete_form) }}
        </li>

    </ul>
{% endblock %}